//holds all the todos and single todo
//initial state
const initState = {
    todos: [],
    todo: ''
}

//actual reduser 
export default (state = initState, action) => {
    switch (action.type){
        default: 
            return state;
    }
}